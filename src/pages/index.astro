---
import Button from "../components/Button.astro";
import GithubIcon from "../components/icons/Github__icon.astro";
import Layout from "../layouts/Layout.astro";
import Roadmap from "../components/Roadmap.astro";
---

<Layout title="Thela Network">
  <header>
    <h1>Thela is <span>Network</span></h1>

    <script>
      //list of txt
      const textos = ["A NETWORK", "A LAYER 1", "A COMMUNITY"];
      let index = 0;

      function changetext() {
        const elemento = document.querySelector("h1 span");
        if (elemento instanceof HTMLElement) {
          elemento.innerText = textos[index];
          index = (index + 1) % textos.length;
          setTimeout(changetext, 3500);
        }
      }

      changetext();
    </script>
    <p>
      Lorem ipsum dolor sit amet consectetur adipisicing elit. Vitae quod saepe
      eum neque velit laudantium. Sequi reprehenderit inventore pariatur iusto,
      deleniti suscipit quasi cupiditate voluptas delectus officiis vitae nemo
      voluptatem voluptatum labore tempora quis harum sint dicta, fuga qui
      beatae accusamus.
    </p>
    <div class="buttons__container">
      <Button href="/">
        <GithubIcon slot="icon-before" />
        GitHub
      </Button>
      <Button href="/">
        <GithubIcon slot="icon-after" />
        GitHub
      </Button>
    </div>
  </header>
  <main>
    <Roadmap></Roadmap>
  </main>
</Layout>

<style>
  header {
    display: flex;
    flex-direction: column;
    text-align: center;
    margin: 3rem 1.5rem;
    gap: 1rem;
  }

  header p {
    font-size: 1.8rem;
  }
  .buttons__container {
    display: flex;
    justify-content: center;
    margin-top: 3rem;
    gap: 5rem;
  }

  main {
    margin-bottom: auto;
    text-align: center;
    padding: 1rem;
    overflow-x: hidden;
  }

  @media (min-width: 765px) {
    header {
      width: 100%;
      text-align: center;
      justify-content: center;
      align-items: center;
      margin: 20rem 0rem 10rem 0;
    }

    header p {
      max-width: 40em;
    }
    body {
      width: 100%;
    }
  }
</style>